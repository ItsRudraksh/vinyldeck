// src/components/VaporGrid/index.tsx
// Dormant legacy renderer. Vapor is no longer a full shell theme; its visual
// energy can return later as a dedicated ambient mode such as Neon Spill.

import "./VaporGrid.css";

export function VaporGrid() {
  return null;
}
